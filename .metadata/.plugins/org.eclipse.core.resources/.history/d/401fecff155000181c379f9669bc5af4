#include "msp.h"
#include "gpio.h"
#define FCPU    240
#include "delay.h"
#include "lcd.h"
#include "keypad.h"


/**
 * main.c
 */

void updateDisplay();

void main(void){
    set_DCO(FREQ_24_MHZ);           //set DCO to 24MHZ
    //lcdInit();                      //initialize display
    initKeypad();                   //initialize keypad
    P1->DIR |= BIT0;
    P1->OUT |= ~BIT0;
    while(1){
        delay_ms(50);
        //updateDisplay();            //update display with most recent key press every 50ms

        if(flag){
            P1->OUT |= BIT0;
        }

    }
}

void updateDisplay(){
    sendCommand(CLEAR);
    sendCharacter(key);
    sendCommand(HOME);
}

